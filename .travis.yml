language: node_js

node_js:
  - 10.15.3

cache:
  directories:
    - node_modules

before_install:
  - npm install -g codecov
script:
  - istanbul cover ./node_modules/mocha/bin/_mocha --reporter lcovonly -- -R spec
  - codecov

  # before_script:
  #   - npm run test

  #script:
  # - npm run test