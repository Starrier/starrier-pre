language: node_js

node_js:
  - 10.15.3

before_install:
  - npm install -g codecov
script:
  - istanbul cover test.js --reporter lcovonly -- -R spec
  - codecov

  # before_script:
  #   - npm run test

  #script:
  # - npm run test